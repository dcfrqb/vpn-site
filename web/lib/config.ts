// Inlined at build time (NEXT_PUBLIC_*). Telegram serves the widget only on the domain set via BotFather /setdomain.
export const TG_BOT = process.env.NEXT_PUBLIC_TELEGRAM_BOT_USERNAME || "crs_vpn_bot";
